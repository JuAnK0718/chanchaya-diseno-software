# Nombre de la aplicacion
spring.application.name=canchaya

# Puerto del backend
server.port=8080

# --- MongoDB ---
# TODO: poner aqui la URI real de MongoDB cuando este lista.
# Mientras tanto se usa el valor por defecto (mongodb://localhost:27017/test).
# El backend arranca sin conexion; solo fallara al llamar un endpoint que use la base.
# spring.data.mongodb.uri=mongodb://localhost:27017/canchaya_db
